---
to: <%- targetDir || 'packages' %>/<%- projectName %>/package.json
---
{
  "name": "<%- name %>",
  "description": "<%- description %>",
  "version": "0.1.0",
  "keywords": [
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=14"
  },
  "author": "<%- author %>",
  "license": "<%- license %>",
  "scripts": {
    "build": "nb-tsc",
    "clean": "nb-clean <%-archiveName%>*.tgz dist *.tsbuildinfo package",
    "pretest": "npm run clean && npm run build",
    "test": "npm run jest",
    "jest": "jest",
    "verify": "npm pack && tar xf <%-archiveName%>*.tgz && tree package && npm run clean"
  },
  "dependencies": <%- h.stringify(dependencies, 2, 2, 1) %>,
  "devDependencies": <%- h.stringify(devDependencies, 2, 2, 1) %>,
  "files": [
    "README.md",
    "dist",
    "src",
    "!*/__tests__"
  ]
}
