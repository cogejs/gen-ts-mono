---
to: <%- targetDir || 'packages' %>/<%- projectName %>/<%- dir %>/index.test.ts
---
import {add} from '..';

describe('testing add function', () => {
  it('5 + 6 = 11', () => {
    expect(add(5, 6)).toEqual(11);
  });
});
